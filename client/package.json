{
    "private": true,
    "scripts": {
        "start": "dotnet fable watch . -o ./public --sourceMaps --run vite serve",
        "build": "dotnet fable . -o ./public --run vite build",
        "clean": "rm -rf javascript/* public/*.js public/*.map public/dist public/fable_modules public/shared"
    },
    "devDependencies": {
        "vite": "^4.1.4"
    },
    "dependencies": {
        "react": "^18.2.0",
        "react-dom": "^18.2.0"
    }
}
